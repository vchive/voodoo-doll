[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "voodoo-doll-world-kernel"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
api = ["fastapi>=0.110", "uvicorn>=0.29"]
test = ["httpx>=0.27,<1"]

[tool.pytest.ini_options]
pythonpath = ["."]
