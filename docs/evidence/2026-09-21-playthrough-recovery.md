# 2026-09-21 · 完整试玩与进度恢复

MW-36 / SP-21 / MW-AC-36 A–F 在本机桌面范围通过。依赖 MW-27–35；不扩写故事，不变更剧情窗口或模型架构。用户要求继续开发和实际游玩；本轮回归矩阵、离线恢复入口与错误处理为实现默认。

## 修复与实际结果

| 问题 | 修改与验证 |
| --- | --- |
| 离线预览联网后被标成在线，已确认本机行动刷新后消失 | 联网刷新仍保留本机分支；菜单“回到在线故事”先保存独立离线备份再恢复服务端进度。备份失败不切换；“离线备份”可导出。原“旧进度”功能保留，离线行动不自动提交 |
| 服务端已提交但确认响应丢失，取消后界面误回旧位置 | 服务端持久回执在重启后仍能识别已执行；取消返回稳定 409，前端取原回执与当前世界。断网取消保留预览，未知预览读取当前会话后解除 |
| 另一标签推进后，旧确认回执覆盖新进度 | 两类确认均比较同 world 的已知在线状态、回执、最新会话，取最高版本；最新查询失败也不丢较新已知状态。旧对白不在新地点演出，历史反馈保留原地点/时刻/名字 |
| 损坏 pending 缺少文字或故事预览，锁死所有按钮 | 缓存标准化丢弃不可恢复 pending，原始坏缓存留在修复备份；按钮和导入入口可继续使用 |
| 离线移动导入后，画面在车站而 YOU 仍在客厅 | 本机移动同步已存在的 YOU/DOLL 坐标；导入旧档以顶层 roomId 统一玩家与娃娃位置，不受过时人物缓存覆盖。NPC 投影规则不变。实际恢复后推荐“返回客厅”，22 次推荐行动可到 trust 结局 |

## 命令与环境

- `npm test`：112/112；`PYTHONPATH=. python3 -m unittest discover -s backend/tests -q`：166/166。
- `npm run typecheck`、`npm run build`、`npm run hex:build`、`python3 -m compileall -q backend`、`node --check app.js`、`git diff --check`：通过。
- 仓库入口：`PLAYWRIGHT_CHANNEL=chrome npm run test:play`。默认也可安装 Playwright Chromium 后运行 `npm run test:play`；没有修改全局浏览器或用户 cookie。
- Desktop Chrome 153.0.8010.52，844×390、667×375、568×320 横屏与 390×844 竖屏。独立随机端口、临时 SQLite、独立浏览器会话，移除子进程 MODEL_/WORLD_ 原配置，不使用模型。
- 报告和失败截图输出 `artifacts/playtest/`；成功/失败关闭自建进程和浏览器，临时数据库保留排查。源码 SHA256、基线提交、每场景请求记录在 JSON 中。

## 浏览器矩阵

完整回归 25/25，累计 234 次推荐行动、1,739 次阅读操作，页面异常为空。计数不包含故障用例通过自由输入发出的行动，也不代表人类阅读时长。

| 范围 | 实际断言 |
| --- | --- |
| 六条完整路线 | trust/station（844）、audit/station（568）、protect/kitchen（667）、trust/kitchen（844）、audit/kitchen（390 竖屏）、protect/station（667）。各 21 次推荐行动，第三日回家；读完最后一句、完成提示、服务端 completed/ending/finalChoice、录音/交班卡按支线取得、刷新保持、可继续探索 |
| 四个错过窗口场景 | 第一日与第三日用明确等待动作得到 durable missed；林川离场、仍可探索，第三日结果可导出导入。第二日错过周野/沈青后通过公开材料或档案继续审计，不补造交班卡/录音 |
| 保存与独立会话 | 途中预览刷新取消、非法输入、坏 JSON 导入保持进度；服务实际重启后同 cookie 恢复。厨房中途档导入另一 cookie 世界后通关，再把完成档导入原世界，双方 worldId 独立、原选择与结局保持 |
| 确认与取消 | 丢确认响应后的重试幂等；故事/行动已提交后重启再点取消恢复真实结果；两类取消断网保留 pending；已经撤回的过期预览可退出 |
| 多标签 | 同浏览器第二标签先恢复共享预览，再推进新行动；原标签分别重试/取消故事和行动，四种组合都不回退。另用同 cookie、独立界面缓存验证已知较新状态在最新查询失败时仍保留 |
| 离线与坏缓存 | 本机 pending 联网刷新、离线确认后刷新、返回在线前备份、备份导出及导入；损坏 intent/story pending 不白屏、不锁死 |

25 项完整回归后，视觉检查发现旧离线导入位置分裂，最后补丁只涉及离线移动坐标与导入坐标优先级。补丁后重跑完整后端 166 项、前端 112 项，并复测直接相关的三个浏览器场景：`save-recovery`、`save-independent-session`、`offline-reconnect`。前两项通过；离线项初次补测错误地要求公开 snapshot 包含逻辑控制者 PLAYER_DOLL，修正测试边界后独立复跑通过，新增“导入后继续到 trust 结局”断言。内核测试另验证 YOU/DOLL 两者位置一致。保留中间失败和最终通过报告，不把它描述为产品再次失效。

本轮主要报告：

- [25 项完整回归](2026-09-21-playthrough-browser.json)
- [最后坐标修复与离线通关补测](2026-09-21-import-position-browser.json)
- [本机 HTTP 与资产校验](2026-09-21-playthrough-http.json)
- 截图：`playthrough-recovery/trust-station.png`、`audit-station.png`、`other-tab-intent-retry.png`、`offline-import-complete.png`。已人工查看横屏正常结尾、恢复提示和竖屏布局；关键行为以请求/状态断言为准。

## 本机更新与边界

18766 已重启到修复版本，沿用 `/tmp/voodoo-review.sqlite3`；最后重启前备份为 `/tmp/voodoo-review-before-final-recovery-20260921-010718.sqlite3`。PID 48576、工具会话 8998 仅为启动记录，接手需重新核对。健康、首页和 15 个 JS/CSS 资产共 16 个响应均为 200，返回字节与本机构建相同。18772 临时服务已停止；自动化服务均已退出。

微信 iOS/Android 真机、真实触摸/软键盘、公网 HTTPS、长篇剧情质量和真实游玩时长仍未验证。旧 v2 的四类剧情问题继续待修。常用自然表达仍有限，例如“观察一下周围”“坐地铁去办公室”尚不能识别，玩家可使用推荐动作或地图；本轮没有以固定路线通关宣称任意开放世界行动已实现。
