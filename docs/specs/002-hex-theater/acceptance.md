# 002 验收 · Pi 与像素展示

2026-09-18。定义沿用[决策 0004](../../decisions/0004-agent-runtime-selection.md)，以下以当前实现证据更新。真实微信与桌面模拟不得混称。

| 编号 | 验收 | 当前状态 |
| --- | --- | --- |
| AR-AC-01 | 干净安装构建、固定 Pi/Team 版本，不加载 coding/TUI/文件/Shell 工具 | **通过**：`npm ci --ignore-scripts`；Pi 0.85.1、Node >=22.19；`npm test` 覆盖工具白名单 |
| AR-AC-02 | 真实网关通过 Pi 完成工具调用→返回→继续；畸形/超时处理 | **部分通过**：本地 OpenAI/Anthropic SSE 与一次真实网关自测；真实网关三幕中一角色 fallback |
| AR-AC-03 | 三角色三轮，公开发言能接话，玩家私信不进 NPC 输入/公开日志 | **通过（自动化）**：角色上下文、定向 mailbox、公开台词与隐私断言 |
| AR-AC-04 | 已确认事实/夜数/舞台重启恢复，取消草稿不提交，确认幂等；旧档保全 | **通过（自动化）**：11 状态测试、10 HTTP 测试覆盖重启/CAS/幂等/迁移 |
| AR-AC-05 | 生成取消、立即重试、迟到响应/工具不落入下一幕 | **通过（自动化）**：取消传播、turnId/epoch、旧 requestId 与迟到 draft 回归 |
| AR-AC-06 | 请求/输出 token/并发/时间上限覆盖重试与环境，不用角色turn数代替 | **通过（自动化）**：budget/metrics 测试覆盖请求、输出、并发、deadline；真实自测 19 请求 |
| AR-AC-07 | 两匿名玩家同名不串世界，未知凭证拒绝，不能凭worldId访问 | **通过（自动化）**：cookie hash、未知会话、同名隔离与静态越权测试 |
| AR-AC-08 | 无模型/网络失败/已载页面断网可演出；角色、房间、动作、ENV约束生效 | **通过（自动化）**：本地脚本、HTTP fallback、契约裁剪、9 前端状态测试 |
| AR-AC-09 | 320/390/430px，输入/确认/取消/键盘流程；微信与公网单列 | **部分通过**：桌面浏览器 390px 主链已走通；320/430 与真机微信/公网仍待记录 |
| VIS-AC-01 | 5种现代日常人物轮廓明显不同；非仅换色；不依赖旧角色图片 | **通过（代码/构建）**：原创五套轮廓、发型、衣着和动作；外部 LPC 仅自测候选 |
| VIS-AC-02 | 行走四方向、说话、待机、寻路和物件交互无回归 | **通过（自动化/构建）**：角色纹理、stage 寻路与物件契约检查；真机帧率待测 |
| VIS-AC-03 | 320/390/430px能辨认角色；构建无donarg；所有新增素材来源可说明 | **部分通过**：构建不复制 donarg，素材来源在 `CREDITS.md`；320/430 与 LPC 自测接入待最终回归 |

## 设备与发布

- 桌面浏览器手机尺寸模拟：390px 主链已测；320/430px 待补。
- iOS/Android 真机微信：未测。
- 公网服务器发布：未执行。

## 证据

自动化证据：`npm test` 46/46；`npm run build`；`npm run hex:build`。真实网关自测：3 幕、19 次请求，重启后夜数恢复，第三幕有 1 个角色走本地 fallback。不得把该自测写成全角色模型成功；微信真机、公网与战报截图仍未验收。
