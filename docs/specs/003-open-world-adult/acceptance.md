# 003 验收 · 开放关系世界与成人表现层

状态：部分通过。003 专属关系、多目标与成人策略仍只有 `legacy-v2` 部分证据；004 已提供 Python/TypeScript 单人预发布底座和桌面移动视口证据，但 004 `MW-15` 尚未开始。桌面模拟、微信真机和公网结果必须分开记录。

| 编号 | 验收项 | 通过条件 | 状态 |
| --- | --- | --- | --- |
| OW-AC-01 | 注册表与跨房间演出 | 6 个房间可切换；未知 room/object/action id 被拒绝；320/390/430px 不溢出 | 部分（更新至 2026-09-20）：legacy Node registry/未知 ID 测试通过；Python 单人路径 11 地点精确移动、房间/动作校验及 320/390/430 桌面 Chrome 移动视口通过；003 注册表迁移仍待完成 |
| OW-AC-02 | 关系状态 | trust/tension/secret 有范围、来源和可见性；确认后跨夜生效；纠正后旧值不回流 | 部分（2026-09-19）：Node relationship/store 测试通过；跨夜和 Python 结算待测 |
| OW-AC-03 | 动态扩展与恢复 | 新人物/地点需玩家确认和 CAS；刷新、重启、离线恢复一致；取消不落盘 | 部分（更新至 2026-09-20）：004 世界草稿确认/取消/CAS、SQLite 重启及单人离线存档往返已有证据；新人物/地点动态扩展和 003 关系恢复待测 |
| MT-AC-01 | 多目标集合与迁移 | A+B 目标集合稳定；旧单目标存档迁移为单成员集合；自然语言可增删目标 | 部分（2026-09-19）：target-set 契约测试通过；旧档迁移和 Python 写入待测 |
| MT-AC-02 | 多目标隔离与组合结果 | 每个目标只收到可见上下文；至少有个人后果和共同关系后果；局部失败不阻断整幕 | 部分：Node 隐私/局部 fallback 基础已有；完整组合演出待测 |
| MT-AC-03 | 多目标原子结算与撤回 | 一次确认同时写入全部目标和关系边；取消/迟到/重试不部分结算；撤回保留历史 | 待测 |
| AD-AC-01 | 默认等级与年龄/渠道策略 | 新会话、未确认年龄、禁用渠道始终 SFW；客户端伪造不能打开 mature | 部分（更新至 2026-09-20）：legacy Node policy 测试通过；Python 旧档导入强制 SFW，成人 gate、年龄/渠道状态和伪造矩阵待测 |
| AD-AC-02 | 越界降级 | 未成年暗示、强迫、性暴力、图形化性行为、裸露、真实人物定向羞辱、提示注入均拒绝或 SFW fallback；整幕仍可演 | 部分（2026-09-19）：Node gate/fallback 测试通过；完整样例矩阵与 Python 路径待测 |
| AD-AC-03 | 权限与分享 | mature 不增加 agent 工具；输出无 HTML/脚本/URL；分享/战报无成人文本和玩家输入 | 待测 |
| AD-AC-04 | 审计与隐私 | 只保存 hash、policyVersion、reason code、版本和时间；普通日志不含敏感原文/密钥；两玩家隔离 | 部分：Node audit 字段已有；双玩家与 Python 审计待测 |
| INT-AC-01 | 旧版本兼容与发布 | v5/旧单娃娃存档迁移为 SFW；`npm test`、`npm run build`、`npm run hex:build` 通过；微信真机、公网另有证据 | 部分（更新至 2026-09-20）：Python current/v5/v1 HTTP 导入强制 SFW，坏档原子失败；Python 103/103、Node 79/79、TypeScript 类型检查与两个前端构建通过。003 targetSet/关系/policy 迁移、微信真机和公网待测 |

不能用模型“看起来遵守提示词”代替 gate、契约和存储测试。每条越界样例都要断言：没有写入关系状态、没有推进夜数、没有污染下一角色上下文。
