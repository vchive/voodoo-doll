# 004 验收 · 多 Agent 世界内核与分阶段迁移

本轮新增（2026-09-20，MW-35 / SP-20 / MW-AC-35）：用户继续要求完善横屏游玩。本轮依据阅读与输入衔接问题细化长句先滚动后换句、滑动不误跳、多选项发现与翻动、中文输入法/长按 Enter 防误提交及面板/预览焦点交接；均为实现默认，用户未逐项指定。依赖 MW-34，已测桌面长句/翻动、三路线及 11 项合成输入/焦点复测通过，前端测试 104/104；整项部分完成，未覆盖组合和微信真机另验，见[阅读衔接证据](../../evidence/2026-09-20-reading-flow.md)。前轮横屏布局通过不代替本轮行为验证。

上一轮横屏（2026-09-20，MW-34 / SP-19 / MW-AC-34）：用户明确要求手机游玩以横屏为主。横屏优先使用可用宽度组织舞台、对话和行动；短屏滚动、次级面板、竖屏提示、方向切换状态保留、安全区与软键盘处理均为实现默认。依赖 MW-33，本轮三档横屏通关、12项方向/面板专项及390×844竖屏/长句/安全区模拟通过，99项前端测试与构建通过，微信真机另验，见[横屏证据](../../evidence/2026-09-20-landscape-play.md)。前轮阅读交互通过记录不代替本轮布局证据。

上一轮交互（2026-09-20，MW-33 / SP-18 / MW-AC-33）：用户反馈“文字太多，不知道看哪里、交互不方便”，并明确要求旁白由巫毒娃娃承担。该轮收敛为单一对话阅读焦点、娃娃叙述、读完接行动和次级信息折叠；推荐动作一次点击由客户端串联既有服务端预览与确认，自由输入保留可见预览。当前本机范围通过99项前端、159项后端测试与三路线/窄屏/键盘/恢复浏览器验证，见[上一轮交互证据](../../evidence/2026-09-20-dialogue-focus.md)。下方159/159、96/96及三条浏览器路线为更早的故事基线，均不代替 MW-34 专项证据。

前一轮故事验证（2026-09-20）：Python 159/159、Node 96/96、类型检查与双构建通过；独立HTTP 220请求覆盖四结局与读档，桌面Chrome完成390×844署名、320×844保护、320×568审计路线。新故事本机闭环通过，整项仍部分完成：长篇内容/时长、微信、公网和完整故障矩阵未完成。见[本轮证据](../../evidence/2026-09-20-signal-story.md)。
更新日期：2026-09-20（Asia/Shanghai）。当前新建世界默认 `templateId=signal-rain-v1`、`version=1`，标题《红灯下的第三次回声》。本轮按用户“去网上找短篇小说融进来”的要求，实际阅读狄更斯《The Signal-Man》英文原文，以其警报、值班和重访结构创作三日教程。正文约 135 句、4,372 个汉字、20 个服务端步骤；这是内容规模记录，不能推导真实游玩时长或宣称已完成长篇。当前实现与验证范围见[本轮证据](../../evidence/2026-09-20-signal-story.md)，来源和改编边界见[研究记录](../../research/2026-09-20-signal-man-adaptation.md)。

`rainy-office-v1` 与 `rainy-office-v2` 原有内容、状态和存档路径保留，不自动升级为新故事。v2 已知的最终决定误触、`missed` 可覆盖、离场人物对白、时间/线索文案不一致四类缺陷仍待修；**兼容保留不等于通过验收**。以前的 Python 145/145、Node 95/95、v1 桌面浏览器/对话窗证据均为历史基线，不代替新模板的专项自动化和实际试玩。微信真机、公网 HTTPS、完整故障矩阵和真实游玩时长未验证。

| 编号 | 验收项 | 通过条件 | 状态 |
| --- | --- | --- | --- |
| MW-AC-01 | 跨语言契约 | OpenAPI/JSON Schema 可生成 TS 类型并由 Pydantic 校验；未知 action、actor、target、channel、relation 被拒绝 | 部分：契约文件已纳入 Git 基线，生成/Pydantic 待做 |
| MW-AC-02 | 身份与控制边界 | `PLAYER_DOLL` 可控制 `YOU`；玩家或模型提交 A/B/C/Z 直控命令返回 422；A/B 可自主拒绝、沉默、离开或反问 | 通过（2026-09-18）：`PYTHONPATH=. python3 -m unittest discover -s backend/tests -v`，16/16；`test_kernel.py`、`test_world_kernel.py` |
| MW-AC-03 | Python 服务 | FastAPI `/healthz`、命令和 SSE 在本地启动；缺少模型配置不阻塞核心流程；密钥不出现在响应和前端包 | 本机通过（2026-09-18）：`PYTHONPATH=. python3 backend/run.py`；curl `/healthz`=200，A/B 多目标 POST 返回 200；公网仍待测 |
| MW-AC-04 | 事件存储与回放 | 一幕可写入快照和 append-only 事件；重启后按固定 resolver 重建相同世界/舞台；重放不调用模型 | 通过领域层：SQLite、CAS、回放测试通过 |
| MW-AC-05 | 能力与权限 | 发起能力、被交互能力、在场、距离、前置物件和关系条件均校验；非法状态不能写入数据库 | 通过领域层（2026-09-18）：物件/动作/房间/同房间校验在 `test_world_kernel.py`，非法输入在提交前拒绝 |
| MW-AC-06 | 可见性隔离 | A 的 private 事件不出现在 B/C/无关玩家上下文；`doll_private` 不进入人物 prompt；public 事件只发给在场者 | 通过领域层（2026-09-18）：private 事件、关系快照 viewer 过滤和 `events_for_viewer` 测试通过 |
| MW-AC-07 | 无模型回退 | 断网、超时、429、5xx、非法 JSON、预算耗尽均使用本地模板完成一幕；角色失败只影响自身回应 | 通过本地路径（2026-09-18）：RuleAgent/ENV deterministic resolver、单角色异常 fallback、16 项 Python 测试；真实 provider 故障矩阵待测 |
| MW-AC-08 | 玩家推动对质 | 玩家输入“让 A 和 B 当面对质”被拆成 YOU 的邀请/询问/施压行动；没有 A 直控 B 的事件；A/B 的回应分别由其 Agent/回退产生 | 通过领域层（2026-09-18）：多目标 ask 事件为 YOU→A、A 自主回应、YOU→B、B 自主回应；无 A→B 直控事件 |
| MW-AC-09 | 微回合原子性 | 发起、目标回应、ENV 反馈、关系和记忆按 turn 原子提交；取消、超时、重复确认、CAS 冲突不产生部分结算或重复事件 | 部分通过（2026-09-19）：SQLite 原子提交、幂等、CAS、turnId 重启复用和 draft/confirm/cancel 通过；OperationQueue 已接同步提案路径，`test_world_kernel.py` 验证 world/activation 变化后的迟到结果拒绝和持久审计；持久 worker、异步超时矩阵待测 |
| MW-AC-10 | 独立人物 Agent | A/B/C 各有独立记忆、目标、立场和能力；相同公开事件可产生不同合法回应；一个 Agent 不读取另一个的私密记忆 | 部分通过（2026-09-18）：Context Builder 只向目标 Agent 提供隔离上下文，A/B/C 具有独立 traits/goals/memory；多轮目标行为待测 |
| MW-AC-11 | Z 生命周期 | Z 可在场景中复用并拥有短期记忆；刷新/换场不自动写长期角色槽位；只有玩家确认注册后才可成为 A–Y 长期角色 | 待测 |
| MW-AC-12 | 旧存档迁移与回滚 | `voodoo-hex-v5`、003 和单娃娃档迁移为 `PLAYER_DOLL`/`YOU` 与单目标集合；头像和已确认事实保留；失败可回到旧 key | 部分通过（2026-09-19）：`backend/app/migrations/legacy.py` 覆盖 v5/v1 只读转换、source hash、头像/事实/房间/记忆保留和显式迁移元数据；`test_single_player_api.py` 覆盖 v5/v1 HTTP 导入及坏档不覆盖当前状态，targetSet、浏览器旧 key 保留与回滚验收待补 |
| MW-AC-13 | TypeScript 手机前端 | 320/390/430 CSS px 下角色、对话、输入、SSE 状态不重叠；断线补拉和重复 eventId 不重复演出；核心交互无 hover 依赖 | 部分（2026-09-19）：`npm run typecheck`、`npm run hex:build`、`node --test tests/world-api.test.js` 通过；`WorldEventReducer` 按 `eventId` 去重，SSE 断线先 `/events?after=` 补拉再重连；320×568、390×844、430×932 三档桌面 Chrome 移动视口仿真完成单人闭环且无溢出/重叠、Canvas 非空；微信 iOS/Android 真机和完整 SSE 事件舞台仍待测 |
| MW-AC-14 | Agent Adapter 与职责路由可替换 | world-builder、player-doll、character、observer 可分别选择 local/Pi/DeepSeek/Claude（已配置者）和预算；所有 provider 通过同一结构化契约，工具权限、世界写入和可见性不随 profile/provider 改变 | 部分通过（2026-09-18）：`StructuredProviderAdapter` 统一 Pi/DeepSeek/Claude 输出、预算和 actor 校验，`ResilientAgent` 自动本地 fallback；职责 profile 路由、真实网关 transport、取消和 429/5xx 矩阵待测 |
| MW-AC-15 | 003 兼容策略 | 默认 SFW；成人 gate、关系/多目标原子结算、审计和分享降级由 Python 服务端执行；越界输入不落盘、不推进夜数 | 待测 |
| MW-AC-16 | 分阶段发布证据 | Node legacy-v2 和 Python 版本均可启动；迁移、重启、回滚后事件一致；微信 iOS/Android、服务器外 HTTP、健康检查分别有真实记录 | 部分通过（2026-09-19）：Python 单人入口、本机健康检查，以及 320×568、390×844、430×932 三档桌面 Chrome 移动视口仿真已有记录；完整迁移/回滚一致性、微信 iOS/Android 真机和服务器外公网/HTTPS 待测 |
| MW-AC-17 | 统一世界时钟 | `/world`、事件和 Agent 上下文使用服务端 `WorldClock`；伪造客户端 `day/minute` 不推进时钟；重启后 `clockVersion` 和单调锚点可恢复 | 部分通过（更新至 2026-09-20）：历史 Python 138/138、Node 93/93 回归通过；`test_lifecycle.py` 覆盖基础投影/clockVersion，API 拒绝客户端时间；管理令牌关闭和授权访问 internal tick 已覆盖，生产节拍配置与跨进程重启实测待补 |
| MW-AC-18 | 时间表与驻留投影 | 每个角色按版本化时间表得到唯一可重放 `PresenceProjection`；重复/例外/重叠按固定规则处理；未激活时不调用模型、不生成逐分钟生活事件 | 部分通过（2026-09-19）：工作/居家投影、半开/跨午夜、重复 tick、默认位置、世界草稿 API 校验和确认日程已有覆盖；非零容差/例外日期明确拒绝，完整冲突审计待补 |
| MW-AC-19 | 兴趣范围与激活租约 | 进入 `roomId/zoneId` 创建有 TTL 的 `ActivationLease`，激活上下文只含时间摘要和可见事件；离开/过期释放租约；重复心跳和重连不重复演出 | 部分通过（2026-09-19）：严格 office/desk 匹配、TTL、重复 heartbeat、过期拒绝、quiescing checkpoint/summary、日程切换、generation/token 和 encounter generation 已覆盖；坐标范围、完整异步迟到结果矩阵和生产后台节拍待补 |
| MW-AC-20 | 可重放随机遭遇 | 激活遭遇来自注册表并记录稳定种子与 encounterId；刷新、补拉、重复 interest 不重抽、不重复写事件 | 部分通过（2026-09-19）：`test_lifecycle.py` 验证稳定选择、activation 事件落盘与 heartbeat 不重抽；`test_world_builder.py` 验证确认候选接入激活；关系条件、离开重入/跨进程重连矩阵待补 |
| MW-AC-21 | 世界观察者边界 | 确定性审计覆盖时钟、时间表、位置、事件和关系不变量；模型观察者只能提交审计/修复提案，不能读取 private、进入角色对话或直接改世界；观察者不可用不阻塞玩法 | 部分通过（2026-09-19）：房间/日程报告脱敏持久化，数据库按 worldId+reportHash 去重；A/B/A、双连接并发、重启、旧库迁移保留历史均有 `test_world_kernel.py` 证据；完整不变量、模型和修复提案待测 |
| MW-AC-22 | 世界搭建确认 | 高能力模型只生成世界圣经、地点/角色/关系/时间表/遭遇注册表草稿，并能对关键缺口返回澄清问题；未确认草稿不进入运行时或人物上下文，确认后有版本化事件且可回放 | 部分通过（2026-09-19）：`test_world_builder.py`、`test_world_builder_api.py` 覆盖实体/日程/遭遇同批发布、预览隔离、取消、CAS、哈希复核、并发确认、SQLite 重启恢复；世界圣经、新房间/角色/关系、澄清问题、高能力模型接入和事件从零重建待测 |
| MW-AC-23 | 原子能力与实体 affordance | 固定 primitive、实体状态和参数 schema 可拒绝未知 tool/entity/action；权限与前置条件可验证，兴趣范围可裁剪 | 部分通过（更新至 2026-09-20）：固定 registry、schema、room/zone、类型/动作/路线检查已有；车辆目的地运行时二次校验有 `test_tool_declarations.py` 证据；不可变定义与可变运行态仍同时投影到 `WorldState`，完整分离和运行时契约待补 |
| MW-AC-24 | 交通与环境交互 | 车辆、地铁、自行车、灯光等通过固定 primitive 和实体数据结算；无人观察的移动使用 TravelProjection，玩家途中进入范围时可一致地物化为组合动作；组合动作可重放，无模型时使用 ENV fallback | 部分通过（更新至 2026-09-20）：`test_tools.py` 验证自行车行驶、地铁 board/travel/alight 顺序、路线/实体类型拒绝、实体位置更新和 ENV mobility feedback；`test_tool_declarations.py` 验证自行车单步和地铁三步组合动作预演及非法顺序/范围拒绝。TravelProjection、途中物化、领域事件重建、班次时间和坐标范围待测 |
| MW-AC-25 | 世界搭建 ToolCompiler | 玩家叙述/高能力模型只生成声明式草稿；静态校验和预演通过；任意代码、网络/文件操作和未注册副作用被拒绝 | 部分通过（更新至 2026-09-20）：`tool-compiler-3` 的实体/公开日程/遭遇类型、范围、重复 ID、引用、schema、预览统计和基础组合动作预演已有证据；新房间/角色、例外日程、图可达性及分支组合待测 |
| MW-AC-26 | 工具提案发布与审计 | 仅玩家确认后的版本化注册表进入运行时；拒绝提案、失败原子性、回放和审计均有证据 | 部分通过（更新至 2026-09-20）：非法声明、存储失败、删除失败、CAS、跨连接取消/确认和并发重试、重启 receipt 有 `test_world_builder.py` 证据；拒绝审计与 ENV fallback 已有；发布事件从零 reducer、mobility 完整回放、失败矩阵和持久 worker 待补 |
| MW-AC-27 | 匿名单人预发布闭环、启动恢复与行动可发现性 | 两个 cookie 对应独立 world，伪造 world/viewer/session 无效；故事与行动先预览再确认/取消，重复确认幂等；移动后只激活新兴趣范围；SQLite 重启与导入导出恢复；旧/损坏本地缓存不导致白屏；当前地点、可到达地点和在场角色有直接可点击且仍经预览/确认的行动；请求处理中有明确状态并阻止重复提交；产品错误中文化；同源服务不暴露源码/密钥，写请求和 internal API 受保护；无模型可完成闭环 | 部分通过（历史基础流程证据截至 2026-09-20，不代表新故事预发布通过）：`test_single_player_api.py` 7/7 覆盖 cookie world 隔离、草稿隔离、preview-confirm-cancel、幂等、移动后激活、角色自主回应、SQLite 重启、当前/v5/v1 导入导出、坏档原子失败和 HTTP 边界；`test_static_serving.py` 3/3 覆盖静态安全边界。历史 Python 138/138、Node 93/93、类型检查及构建通过。320×568、390×844、430×932 基础闭环已有桌面 Chrome 证据；390×844 增量验收覆盖旧/坏缓存恢复、快捷行动、全部 11 地点精确移动、取消无副作用、防重复、中文错误、离线导出到在线导入、cookie 变化后本机进度不被覆盖、故事预览刷新恢复、坏导入后表单解锁、故事确认响应丢失后刷新恢复，以及导入/导出期间冲突控件锁定与输入保护。完整 viewer/session/private 越权矩阵、生产配置、微信 iOS/Android 真机、公网/HTTPS 和内容丰富度待测 |
| MW-AC-28 | 小说结构改编教程、世界模板与时间分支（SP-11/SP-14） | 新会话确认 signal-rain-v1《红灯下的第三次回声》，三日内分别从周野/沈青入口完成 trust/audit/protect；第一日任何早期阶段过10:00、第三日过12:00均complete+missed。第二日周野错过可实际查公开交班簿，沈青错过不得取得录音。取消/幂等/重启/导入恢复，旧档不升级；无模型可玩；结尾仅引用已得证据 | 部分完成；14项专项、HTTP四结局及三路线桌面浏览器已通过，见本轮证据；长篇与真实时长未验，v2兼容不等于通过 |
| MW-AC-29 | 推荐行动、自由输入恢复与主角生活（SP-12/SP-13） | signal推荐与自由输入共用确认；当前物件开门有真实反馈，但不凭普通开门/等待获取调查证据；档案校对显示耗时并持久保存。回家不跳日，home的sleep1/2才可显式跨日；第三天睡觉意图可解析，错误地点/错日拒绝。导入恢复道具，旧故事不残留signal描述；错误后能直接行动 | 部分完成；新模板推荐操作、睡眠耗时和读档续玩已通过，见本轮证据，手机/微信/公网分别验收 |
| MW-AC-30 | Galgame 对话窗与阅读状态（SP-15） | signal各步骤的角色名牌/对白/YOU心声/环境可辨；只显示在场人物，按事实投影材料和缺口。点击/键盘/回看/提前行动/刷新恢复不提交世界动作，world/dialogue游标隔离；320/390无遮挡 | v1展示底座有历史本机通过证据；signal新稿在场/证据投影自动化及320/390桌面阅读通关已通过，微信真机待测 |
| MW-AC-31 | 事实推进、明确选择、上下文与错误恢复（SP-16） | v1非标准顺序纠偏保持兼容；signal不补未读证据，receiptSeen只在第三日核验后成立；day3仅向A明确ask/tell选择，audit需auditSaved、trust需manualRed+dossierRead。普通行动不选路线，completed+missed不可覆盖；离场接话拒绝，错误原文/记录保留并显示行动出口 | 部分完成；signal专项边界与导入恢复已通过，见本轮证据。v2四类已知缺陷未修；旧测试通过不代表新行为已验 |
| MW-AC-32 | v1 教程第二天旧伞支线与旧档恢复（SP-17 / MW-32） | 仅 v1 旧主线结局后可进入第二天旧伞支线；周野在场时可询问，离场后可通过失物登记继续，办公室卡片和回家开门完成支线；支线不改写旧主线 `ending`，预览/取消无副作用，导出/导入和缺少 `postscript` 的旧 v1 存档可恢复，无模型可完成。v2 第二日 station/kitchen 分支不属于本验收项 | 服务端实现与自动化通过：Python 138/138、Node 93/93；`test_narrative.py`（34/34）覆盖 v1 支线入口、当前位置自由行动并存、在场/离场替代路径、办公室卡片、回家开门、导入导出、旧档兼容、无模型完成和主线结局保持。支线浏览器 320/390、微信真机、公网 HTTPS 及断网/丢响应/存储失败矩阵证据待补
| MW-AC-33 | 单一阅读焦点、娃娃旁白与便捷行动（SP-18 / MW-33） | A–E 子项全部提供本轮证据；单一对话焦点、正确叙述身份、读完接行动/信息折叠、推荐一次点击仍经服务端预览确认、自由输入预览及恢复保持 | 本机范围通过；本轮99/159、三路线、键盘与故障证据见交互证据页；微信真机待测 |
| MW-AC-34 | 横屏优先、短屏交互与方向状态保留（SP-19 / MW-34） | A–F 子项提供本轮专项证据；横屏游戏视口/内部滚动、次级面板关闭、方向状态不丢、竖屏兼容与安全区/软键盘可达性按实际环境逐项验证 | A–E及F桌面模拟通过，见本轮横屏证据；F微信真机待测 |
| MW-AC-35 | 阅读、选项翻动与输入焦点衔接（SP-20 / MW-35） | A–E 逐项提供本轮证据；长句不跳过未读区域、滑动不换句、多选入口可发现、翻动零提交、组合/重复 Enter 无误预览及焦点交接不误确认 | 部分通过：已测长句/合成拖动、短屏翻动、三路线和 11 项合成输入/焦点复测通过；未覆盖组合、导入重建补丁复测及微信真机待补，见 reading-flow |

### MW-AC-33 验收子项

| 子项 | 通过条件 | 状态 |
| --- | --- | --- |
| A · 阅读焦点 | 阅读时仅当前对话窗为主要文字区，长目标/passage不重复铺开；地点/时间简短可见，320/390宽度无横向溢出或关键控件遮挡 | 本机范围通过，见交互证据页；真机待测 |
| B · 娃娃叙述 | narration名牌及本段回看使用当前娃娃名，NPC speech保持本人，YOU thought保持心声；无环境/旁白匿名标签抢占叙述身份，无NPC私密数据泄露；原事件actor不被改写 | 本机范围通过，见交互证据页；真机待测 |
| C · 阅读接行动 | 读完后直接看见当前推荐，最后一句/展开不自动选动作；提前行动/错误恢复仍可达；日程、笔记、行动记录默认折叠且可主动展开，展开/折叠不提交动作 | 本机范围通过，见交互证据页；真机待测 |
| D · 推荐快捷执行 | 一次点击明确推荐，客户端依次intent预览与confirm，不要求第二次相同意图确认；耗时/处理中可见，快速双击仅一个有效回合。预览失败不确认，过期确认不得绕过时间窗/在场/版本，失败不自动改选 | 本机范围通过，见交互证据页；真机待测 |
| E · 自由输入与恢复 | 自由输入仍可见预览、确认/取消，取消不变更；未因此前快捷动作自动确认新输入。刷新、回看、折叠不执行未选动作；失败保留原文和恢复出口，390×844/320×568实际操作可用 | 本机范围通过，见交互证据页；真机待测 |

### MW-AC-34 验收子项

| 子项 | 通过条件 | 状态 |
| --- | --- | --- |
| A · 横屏游戏视口 | 844×390、667×375、568×320 使用横屏可用宽度呈现舞台和当前对话/选择；地点/时钟/菜单简短可见。正常阅读与选择不依赖外层长滚动，无横向溢出、关键内容遮挡 | 本轮桌面范围通过，见横屏证据 |
| B · 短屏与长内容 | 长句、长选择及面板内容能内部滚动至末尾；继续/返回/关闭可达，关键控件触控高度至少 44 CSS px；不靠裁掉文字/按钮适配，键盘连按不误提交推荐 | 本轮桌面范围通过，见横屏证据 |
| C · 次级面板与恢复 | 自由行动、手记/日程、回看可打开和关闭；关闭恢复原阅读/选择状态。自由输入、可见预览/确认/取消和错误恢复在横屏可达，面板操作不产生世界动作 | 本轮桌面范围通过，见横屏证据 |
| D · 方向切换状态 | 阅读中、输入未发送、intent pending 三种状态分别横竖往返；worldId/dialogue 游标、草稿、turnId 及已确认剧情保留；旋转不产生 intent/confirm、不重复提交。pending 后续主动取消/确认正常，自然时间不因旋转冻结 | 本轮桌面范围通过，见横屏证据 |
| E · 竖屏兼容 | 390×844 可阅读、提前行动、选择、输入、打开/关闭面板；只轻提示横屏，无强制锁屏或 DOM 旋转，横屏主设计不把竖屏入口挡住 | 本轮桌面范围通过，见横屏证据 |
| F · 安全区与软键盘 | 横屏四边安全区不遮挡状态/按钮；输入聚焦和键盘开合后内容及预览/确认/取消可达，草稿/pending 不丢。桌面视口/安全区模拟、真实 iOS/Android 微信输入法分别标注结果，模拟不能标真机通过 | 桌面视口/安全区代理通过；iOS/Android微信真机待测 |

### MW-AC-35 验收子项

| 子项 | 通过条件 | 状态 |
| --- | --- | --- |
| A · 长句继续 | 有未读下方内容时“继续”只向下滚动一屏，句索引不变；抵达底部后新一次继续才换句，短句正常。连续操作和方向/尺寸变化不跳过未显示正文，提前行动仍独立可达 | 部分通过：568/844 长句分别 8/4 页后才换句；667 长句、方向/连点/提前行动组合待补 |
| B · 滑动与点击 | 指针/触摸滑动及滚轮只滚动文字，滑动结束不误跳句；之后独立点击仍能按 A 继续。记录实际事件/位移/索引，桌面模拟与真机触摸分开 | 部分通过：568/844 合成 40px 拖动后 click 不换句；滚轮、正文独立点击恢复与真机触摸待补 |
| C · 更多选项 | 短屏下方有选项时提示清楚，可点击翻动列表并找到末项；翻动不选动作、不发 intent/confirm。到末尾与列表/尺寸变化后提示同步，主动点击具体推荐仍只执行一次 | 已测范围通过：568 短屏翻动 3 次、零 POST、末尾提示消失；三档推荐各 21 次单回合。其他尺寸/提示组合待补 |
| D · 组合输入与 Enter | 中文输入法组合/确认候选字、长按 Enter 的 repeat 不提前发送预览；组合结束后新操作可提交。一次明确提交只发一份 intent，不因焦点切换顺带 confirm；实际输入法覆盖范围单列 | 桌面合成事件通过：组合/229/repeat 零 intent，组合结束后新 Enter 为 1 intent/0 confirm；真机输入法待测 |
| E · 面板/预览焦点 | 三类面板打开焦点进入，关闭回原入口；预览焦点在非执行容器，取消回阅读/选择非执行容器且保留原文与游标。按住 Enter 不穿透到确认或推荐，焦点交接本身零行动请求；横竖可达 | 部分通过：568/844 预览/取消保持焦点与草稿，自由行动开关、故事库往返通过；其余面板、入口重绘、竖屏 pending 与导入重建待补 |

## 关键手工用例

### 用例 1：A/B 当面对质

1. 玩家在房间内输入“让 A 和 B 当面对质”。
2. 检查服务端只产生 `YOU` 的公开邀请/询问行动；没有客户端可选的 A/B 控制按钮。
3. A 和 B 各收到自己的可见上下文，分别返回同意、拒绝、撒谎、反问、沉默或离开中的一种合法结果。
4. ENV 只反馈房间可观察变化；关系和记忆在同一 turn 原子提交。
5. 检查回放和刷新后结果一致，重复确认不重复结算。

### 用例 2：私密性

1. A 向 YOU 发送 private 事件，B/C 不在场。
2. 检查 B/C 的 prompt、SSE 和记忆没有该事件；DOLL 可见的玩家私密意图也不出现在 A/B prompt。
3. 断线补拉后只返回当前 session 可见事件。

### 用例 3：无模型与迟到结果

1. 禁用模型或模拟超时，发起多目标微回合。
2. 每个目标都使用本地 fallback，ENV 仍反馈，整幕可完成。
3. 迟到的模型结果被丢弃，不能覆盖已提交 fallback；事件日志记录来源和原因。

### 用例 4：统一时钟与驻留

1. 以固定 `WorldClock` 启动世界，尝试在 `/world`、interest 和 turn 请求中传入伪造 `day/minute`；服务端忽略或拒绝该字段，时钟只按服务端锚点推进。
2. 为 A 配置 09:00–17:00 在 `office/desk` 的时间表，为 B 配置相同时间的 `home/kitchen`；在没有玩家会话进入时，检查只有 `PresenceProjection` 事件，没有 A/B 模型请求或逐分钟生活事件。
3. 玩家从 `hall/lobby` 进入 `office/desk`，检查 A 只激活一次，收到从 checkpoint 到当前时钟的摘要；重复 heartbeat、刷新和 SSE 补拉不重复触发遭遇。
4. 玩家离开 `office/desk` 或让租约过期，检查 A 进入 `quiescing` 后释放；A 的公开摘要可回放，私密记忆不会流入玩家或 B 的上下文。

### 用例 5：观察者与修复

1. 注入未知房间、重叠时间表和过期租约，检查确定性 observer 产生带依据事件和版本的 `audit`。
2. 提交一个模型 `correction_proposal`，尝试直接修改关系、事实或角色意志；Kernel 拒绝越权部分，只允许注册表投影、未知事件丢弃和租约清理等确定性修复。
3. 禁用观察者模型，检查时钟、驻留投影、激活回合和本地 fallback 仍可完成，且未产生隐式后台角色推理。
4. 让世界保持无人且没有新事件，跨过多个观察间隔后检查没有模型审计调用；产生多个密集事件后只形成一次合并审计，且只读取脱敏公开摘要。

### 用例 6：匿名单人预发布

1. 用两个干净浏览器上下文访问同源入口，检查各自收到 `HttpOnly`、`SameSite=Lax` cookie 和不同 world；将 A 的 `worldId`、`viewer` 或会话路径放入 B 的请求，B 仍只能得到自己的状态。
2. 输入娃娃名、角色名和世界叙述；预览阶段刷新并核对世界版本不变。分别测试取消和确认；对同一 `draftId` 重复确认只返回第一次回执。
3. 输入“去办公室”，先检查行动预演，再确认移动；快照进入办公室且兴趣范围出现 A。随后询问 A，回应事件由 A 产生，不出现玩家直控 A。
4. 重启 API 进程并保留 SQLite 与 cookie，检查资料、位置、版本和确认回执恢复。导出后导入当前格式，并分别验证 v5/v1 受支持样例；坏 schema 不覆盖现有状态，旧浏览器 key 不删除。
5. 从同源根路径加载构建页面与资产；请求源码、`.env`、SQLite、目录和路径穿越均返回拒绝。跨站 Origin 写请求、超限 JSON 和无/错管理令牌的 internal 请求均被拒绝。
6. 在没有模型配置下完成首次叙述、移动、询问和刷新；分别记录桌面 320/390/430 CSS px、微信 iOS/Android 和服务器外部 HTTP，未执行的环境保持“待测”。
7. 分别注入可迁移旧缓存、截断 JSON、字段缺失和不兼容值后刷新；页面先显示加载/恢复状态，不出现白屏或未捕获异常。旧缓存进入迁移预览；坏记录隔离后恢复 cookie 所属世界，服务端不可用时显示可重试的首次进入界面。
8. 进入主游戏后核对当前地点、可到达地点和在场角色均直接可见且可点击；点击地点进入移动预览，点击角色进入询问/互动预览，取消无副作用，确认后仍由 Kernel 复核可达性、在场和版本。
9. 对故事生成、行动预览、确认、取消、恢复和导入连续快速点击；每次进行中均有清晰状态，冲突控件禁用，同一操作只发出一次有效请求且只产生一个权威回执。模拟 409、422、429、503、断网和未知错误，界面显示中文说明与可执行恢复动作，不展示英文协议 detail、原始异常或堆栈。

### 用例 7：开放世界搭建、模型分层与环境交互

1. 玩家叙述一组人物、关系、地点和冲突；世界搭建模型返回世界圣经、地点图、角色目标/秘密、日程和遭遇草稿。关键事实缺失时只返回澄清问题，不自行发布事实；预览取消后运行时不可见，确认后产生版本化发布事件。
2. 用不同测试 adapter 配置 world-builder、player-doll、character 和 observer profile；检查调用只发生在对应职责和时机，切换 provider 不改变可见性、工具目录和写权限。禁用所有模型后，受限模板和本地规则仍能完成建世界与一轮移动/交互。
3. 玩家未靠近时，角色按日程从家到公司只生成 PresenceProjection/TravelProjection，不调用人物模型或逐步表演吃饭、如厕、工作。玩家在路途范围进入时，同一行程 ID 被物化为可交互场景；无人观察时到点只更新驻留投影。
4. 让角色开车、乘地铁、骑车和开灯；检查模型只提交固定 primitive 加实体参数，Kernel 结算路线、班次、时间和状态，ENV 返回可观察反馈。提交动态函数名或未发布机制时被拒绝并审计。
5. 运行观察者后，检查其只收到脱敏公开摘要且只产生 audit/correction proposal；普通游玩界面不把观察者伪装成角色与玩家对话，需要玩家决定的问题进入新的世界修订草稿。

### 用例 8：新故事三日、证据与窗口（MW-AC-28）

1. 独立会话确认《红灯下的第三次回声》，templateId应为signal-rain-v1；预览/取消不推进。按推荐动作走完正常路线与三个明确结局。
2. 分别走第二日周野/沈青；只有前者取得manualWarning/handover-06-17，只有后者取得tapeHeard/shen-tape。结尾展示对应材料，不能同时声称两条证据均在。
3. 第一日arrival、meeting、day1-choice分别越过10:00，第三日archive/decision分别越过12:00，断言complete+missed且后续行动不可覆盖。记录结算跨界的时间点，预览不能锁定窗口。
4. 第二日周野到11:00离开后，实际查看公开交班簿才能获得事实；沈青到12:00离开后转档案，录音事实仍为false。不能展示离场NPC新台词。
5. 回家不跳日；确认sleep1/sleep2才进入下一天，随后时钟与人物日程一致。刷新、SQLite重启、导入到全新世界都保留状态和可用道具；不清用户旧档。
6. 保留旧v1/v2存档路径，切换时清理signal物件/描述，不把旧模板迁移为signal。v2已知缺陷单列，不能因为能导入而标通过。
7. 无模型运行以上路径；浏览器390×844实际完成正常/迟到，320×568检查长句和按钮，记录阅读耗时。微信真机、公网与故障矩阵分别留待测。

### 用例 9：推荐动作、自由表达与主角生活（MW-AC-29）

1. 进入各章时核对当前目标、今日职责、世界时间、公开行程与上下文推荐动作；玩家无需猜命令即可继续，仍能看到自由输入入口。推荐动作随场景/阶段变化，已离开的人物不能仍作为可当面交谈对象。
2. 到地铁站，输入“去开门，看看里面有啥东西”；预览应指向当前场景玻璃门的打开动作，确认后有具体的门后场景/告示反馈，不得返回“房间保持安静”或无关的地点移动。再取消一次物件动作，核对门和章节保持原状态。
3. 输入一个当前没有注册的物件或动作；检查没有伪造的成功、移动、观察或章节事件。界面保留原文，给出中文原因和可点的替代动作；点击替代动作可恢复游玩。人物自发沉默时有动作/表情反馈，不以空白回应截断剧情。
4. 核对主人公生活提示含校对工作、办公室及完成状态；到办公室使用办公桌完成实际工作。预览显示预计耗时，确认后世界时钟推进且职责完成；刷新/导出导入后仍完成。重复确认不再扣时间或重复结算，取消工作预览不完成任务。
5. 用等待和绕路错过林川的交谈窗口，检查迟到结果不阻塞尚未完成的工作、移动、物件交互、保存和自由输入。世界时间与人物公开日程保持一致。
6. 在 390×844 验证推荐动作、输入错误与恢复；320×568 检查目标、生活状态、行动、输入框、确认/取消均可见可触，不依赖横向滑动或 hover。记录控制台/网络错误；实际执行与未测内容分开填回本表。

### 用例 10：Galgame 对话窗、行动与恢复（MW-AC-30）

1. 在独立世界开始《红灯下的第三次回声》，核对多句剧情进入文字窗，角色名牌与当前说话者一致；公开对白、主人公心声和环境叙述有可理解的区分。`speech` 只含台词，第三人称动作不套进人物引号内。检查公开 guidance 的 `dialogueId` 与 `{speakerId, kind, text}`，没有 NPC private memory 或其他世界数据；`thought` 仅属于 YOU。
2. 点击“下一句”并使用键盘阅读至本段结束；焦点在输入框时输入或按键不会推进文本，也不会确认行动。阅读过程中核对章节/工作/事件没有额外变更，未发生行动确认请求；世界自然流逝与行动耗时分开判断，不将浏览器阅读当成暂停世界。
3. 本段读完后出现对应推荐选项；按MW-33一次点击串联服务端预览/确认。自由输入的可见预览期间阅读控件锁定，取消不提交章节并保留原阅读位置。未知输入被拒绝后保留文本与当前对白，可恢复行动。另在读完前展开行动，能够及时移动、等待或交谈，不能因未读完文字而把时间敏感操作藏成死路。旧选项在时间窗过期后确认仍被拒绝或进入正确迟到分支。
4. 读到中间一句后刷新，恢复同一 world/对话的当前位置；确认新动作后进入新段落，不错误复用上一段的游标。打开“回看”只见本段已显示到当前位置的文本；切换世界/重新开始故事后不带入前一世界的记录或位置。损坏阅读缓存时仍可从本段开头阅读和继续行动。
5. 在 390×844 实际阅读开场、人物对话、选择和结局；320×568 核对长句、名牌、下一句、记录、推荐选项、自由输入和确认/取消可见可触，无横向溢出或遮挡。无模型时同样可读可玩，记录浏览器控制台错误与实际步骤；微信真机单独登记。

### 用例 11：历史非标准顺序回归及新模板边界（MW-AC-31）

以下1–3的原有非线性纠偏用v1独立旧档回归，不声称signal已有相同结果。新模板另验证普通聊天/开门/等待不选择结局，不补录音/交班卡，迟到结果不可覆盖。

1. 在新世界开始教程，先确认“开客厅门”再确认“阅读桌上便笺”；检查服务端以已确认的开门事件推进，不再次要求开门，也不因阅读顺序不同卡在出发目标。另一路线确认合法移动离开客厅后刷新，检查不重新要求“出发”；对比事件、时钟、未读线索和章节状态。
2. 准备一个停在旧章节但已完成开门/离场事实的存档，触发服务端纠偏；检查产生单一可审计、可重试的修正记录，行动时间、未读便笺/告示、已错过窗口和既有结局均不改变，重复请求不再追加事件。
3. 到地铁站在周野仍在场时分别询问日常、站内告示、“三分钟”以及后续追问；核对每个主题返回具体事实或明确下一句，重复询问不会只返回相同空泛占位。离开地铁站后再追问，检查在场校验拒绝旧对象并给出当前可交谈人物/动作。
4. 在只有周野一名可交谈对象时输入“说什么”“什么意思”“然后呢”“继续说”等表达，检查自动关联周野并仍经预览/确认；同时让多个角色同场后输入同样表达，检查先要求选择对象，不猜测目标。完成一段对话后离场，再输入接话，检查不会调用已离场角色。
5. 输入“打开不存在的暗门”或其他未注册行动；检查不会转成聊天、观察、移动或成功事件，原文和用户记录保留。错误面板立即显示当前推荐动作、具体可交谈人物和恢复入口，即使对白尚未读完也能点击继续；选择入口后按正常预览/确认恢复。
6. 在 390×844 与 320×568 分别执行上述路线，检查错误、推荐动作、人物入口、文字窗和确认/取消不互相遮挡；记录实际事件、请求和控制台错误，未执行的微信真机/公网/故障矩阵另列待测。

### 用例 12：从阅读到行动（MW-AC-33 A–E）

1. 在独立测试世界开始新故事，以自定义娃娃名阅读开场；核对同屏主焦点、narration名牌和回看。再阅读NPC对白与YOU心声，确认没有全改成娃娃身份。截图记录390×844和320×568。
2. 默认收起日程/笔记/行动记录；展开再折叠，检查没有intent/confirm请求和剧情推进。逐句读完后找到推荐动作；只读最后一句不会执行。提前行动仍可达。
3. 点击推荐观察或移动一次，记录一组intent和confirm及唯一确认回执，页面进入下一段而不要求再确认。快速双击同一个或其他推荐，检查只有所选动作有效且状态/耗时反馈清楚。
4. 模拟预览失败、确认时版本/在场/窗口过期及连接丢失，检查没有越权提交、无自动换动作/重复确认；错误出口保持可见，不用重新读完全文才能继续。
5. 在一次快捷行动后输入自己的对话或动作，确认出现可读预览；先取消，世界版本/地点/章节不变且输入保留，再手动确认一次。刷新、回看或折叠不替用户确认 pending。
6. 正常路线重复上述操作并读到NPC与结局，记录实际点击次数/请求/截图和发现的问题；自动点击运行耗时不当作人类阅读时长。无模型、桌面移动尺寸、微信真机、公网与故障矩阵分别记录。

### 用例 13：横屏游戏视口、面板与方向恢复（MW-AC-34 A–F）

1. 使用独立测试会话，分别在 844×390、667×375、568×320 开始或恢复故事，阅读开场、人物对白和一段较长叙述，再进入选择。记录舞台/文字区域、页面与面板滚动范围、按钮边界及截图；关键控件无需滚动外层长页面寻找，无横向溢出和裁字。
2. 在最矮视口滚动长对白/多个选项至末尾，执行继续、提前行动、返回对白；关键控件高度至少 44 CSS px。按住 Enter 读到末句不自动提交推荐，主动点选只完成一个既有 intent→confirm 回合。
3. 分别打开自由行动、手记/日程、回看，再关闭回到同一段落/选择。输入有效行动查看预览，先取消、再主动确认；输入未注册动作后通过可见恢复入口继续。面板开关/阅读/滚动没有提交世界动作。
4. 在读到中间一句时从 844×390 切为 390×844，再回横屏；另在输入未发送文字与已有 pending 的状态重复。比较 worldId、dialogueId/index、输入原文、pending turnId、章节/版本及请求日志；只改布局，不重开世界、不自动确认。pending 旋转后分别验证取消无副作用、确认仅一次。
5. 390×844 单独验证主要阅读、选项、输入、次级面板及故事库往返可用；横屏提示不阻断操作，不使用强制方向锁或旋转 DOM。普通页面尺寸变化不误当作重新开始。
6. 模拟四边安全区和缩小可视视口，聚焦输入、输入中文、展示预览并取消/确认；入口不能落到不可滚动的裁剪区域。桌面模拟仅说明布局代理结果；微信 iOS/Android 的实际横竖旋转、刘海区、浏览器栏和软键盘开合另列真机待测。
7. 填写本轮命令、浏览器版本、页面资产/源码标识、独立服务与数据位置、DOM/请求报告及截图。保留用户 18766 会话/数据；本机证据不证明公网部署、长篇完成或微信真机通过。

### 用例 14：阅读滚动、选项发现与输入焦点（MW-AC-35 A–E）

1. 在 568×320、667×375 准备超过一屏的当前对白，记录文字区的 scrollTop/clientHeight/scrollHeight 与句索引。第一次继续只下滚，重复到文字底部仍不跨句，再一次明确继续才换句；以短句作对照。滚动中连点、改变方向/高度后重试，正文不得被跳过，提前行动仍能主动进入选择。
2. 在文字上执行拖动/触摸滑动，再松手，记录没有换句和世界请求；滚轮同样只滚动。随后独立点击可正常继续。桌面合成指针与真实触摸的测试范围分别填写。
3. 在短屏出现多个推荐动作，检查“下方还有选项”提示和可点翻动入口。点击翻动找到末项，只改变列表滚动，不产生 intent/confirm；末尾不继续提示下方还有内容。回到上部或切换尺寸/段落时提示重新反映真实溢出；主动点选具体动作只执行一次。
4. 自由输入中文时覆盖 compositionstart/update/end 与候选字 Enter，组合期间不预览；组合结束后新一次 Enter/预览按钮可提交。按住 Enter 产生 repeat 事件不连发，输入提交后即使继续按住也不确认。分别记录输入原文、intent/confirm 数量及桌面模拟/实际输入法环境。
5. 使用键盘打开自由行动、手记/日程、回看，焦点进入可见面板；关闭回到开启入口。创建行动预览后焦点位于非执行容器，主动取消后焦点回原阅读或选择容器；保持输入/游标，不能因连续 Enter 顺带执行。原入口被重绘时应有可达容器作回退。
6. 在 844×390 与 390×844 做短句、面板、自由输入和 pending 取消回归，确认布局/状态不退化。记录每项截图、焦点/滚动/索引与请求报告；微信 iOS/Android 的实际中文输入法及触摸未执行则待测，不以本轮仿真代替。

## 证据要求

任何“完成”状态必须附：测试命令和输出摘要、事件/快照样例、必要的网络 payload 脱敏记录、桌面尺寸截图或真机记录。未执行项目保持“待测”，不得以模型演示、构建通过或提示词效果代替 Kernel、隐私、事务和回放证据。

## 本轮验收结论

MW-AC-35 当前部分通过：桌面已测长句/合成拖动、短屏列表翻动、三路线共 455 次阅读，以及 11 项合成输入/焦点复测和 104/104 前端测试通过。未覆盖组合、导入重建清理补丁复测与微信真机保留待测，实际范围见[reading-flow](../../evidence/2026-09-20-reading-flow.md)；前轮通关/布局不代替。

MW-AC-34 A–E及F桌面模拟通过，微信真机待测。具体视口/交互结果见[横屏证据](../../evidence/2026-09-20-landscape-play.md)；此前竖屏和阅读交互验收不能替代。

MW-AC-33 A–E 本机桌面浏览器范围通过；真机/公网及完整故障矩阵待测，实际结果见[交互证据](../../evidence/2026-09-20-dialogue-focus.md)。用户新的可读性/便捷性反馈不能被旧通关结果覆盖。

前一轮signal-rain-v1的MW-AC-28–31已取得本机故事与专项通过证据、整项仍部分完成；实际命令、HTTP、浏览器路线与未测项以[2026-09-20-signal-story.md](../../evidence/2026-09-20-signal-story.md)逐项记录为准。约135句/4,372汉字和构建成功不能证明故事质量、长篇时长或预发布可用性。

v1对话窗/非线性纠偏与MW-AC-32旧伞支线保留历史范围；v2最终选择误触、missed覆盖、离场对白、时间/线索文案问题仍未修。旧模板兼容不代表验收通过。微信iOS/Android真机、公网HTTPS、完整故障矩阵和实测完整阅读时长未完成。
